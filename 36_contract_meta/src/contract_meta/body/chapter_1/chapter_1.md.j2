# 제1장 개요

## 1.1. 사업의 개요 및 특성

### 1.1.1. 용역 대상 공사의 개요

| 구분 | 내용 |
|---|---|
| 공사명 | {{ project.name }} |
| 계약체결일 | {{ total_contract.initial.date | kdate }} |
| 발주자 | {{ owner.name }} |
| 계약상대자 | {{ contractor.name_display | default(contractor.name_legal) }}{% if contractor.share_ratio_percent %} {{ contractor.share_ratio_percent | int }}%{% endif %} |
{% if supervisor and supervisor.raw_text %}| 건설사업관리단 | {{ supervisor.raw_text }} |
{% endif %}{% if scope %}| 공사의 주요 내용 | 1. 공사 규모<br>{% for it in scope['items'] %}　● {{ it }}<br>{% endfor %}2. 공사 위치<br>　● {{ scope.location }}{% if scope.stations %}<br>　※ {{ scope.stations }}{% endif %} |
{% endif %}| 총공사 계약금액 | 최초: {{ total_contract.initial.amount.korean | default('') }} (₩{{ "{:,}".format(total_contract.initial.amount.krw) }}){% if total_contract.revisions %}<br>최종: {{ total_contract.revisions[-1].amount.korean | default('') }} (₩{{ "{:,}".format(total_contract.revisions[-1].amount.krw) }}){% if total_contract.revisions[-1].amount_diff_krw %}<br>**중 {{ "{:,}".format(total_contract.revisions[-1].amount_diff_krw) }}원**{% endif %}{% endif %} |
| 총공사 기간 | 최초: {{ total_contract.initial.period_start | kperiod(total_contract.initial.period_end) }} ({{ total_contract.initial.duration_days }}일){% if total_contract.revisions %}<br>최종: {{ total_contract.revisions[-1].period_start | kperiod(total_contract.revisions[-1].period_end) }} ({{ total_contract.revisions[-1].duration_days }}일){% endif %} |
| 1차수 계약금액 | {{ first_year_contract.initial.amount.korean | default('') }} (₩{{ "{:,}".format(first_year_contract.initial.amount.krw) }}) |
| 1차수 공사기간 | 최초: {{ first_year_contract.initial.period_start | kperiod(first_year_contract.initial.period_end) }} ({{ first_year_contract.initial.duration_days }}일){% if first_year_contract.revisions %}<br>최종: {{ first_year_contract.revisions[-1].period_start | kperiod(first_year_contract.revisions[-1].period_end) }} ({{ first_year_contract.revisions[-1].duration_days }}일)<br>**중 {{ first_year_contract.revisions[-1].duration_days - first_year_contract.initial.duration_days }}일 연장**{% endif %} |

## 1.3. 계약체결 및 변경 계약의 경과

### 1.3.1. 1차수 계약 현황

| 구분 | 계약일 | 계약기간 | 일수 | 증감일 | 계약금액(원) | 사유 |
|---|---|---|---:|---:|---:|---|
| 최초 | {{ first_year_contract.initial.date | kdate }} | {{ first_year_contract.initial.period_start | kperiod(first_year_contract.initial.period_end, sep='~') }} | {{ first_year_contract.initial.duration_days }} | - | {{ "{:,}".format(first_year_contract.initial.amount.krw) }} | 최초계약 |
{% for r in first_year_contract.revisions %}| {{ r.seq }}회변경 | {{ r.date | kdate }} | {{ r.period_start | kperiod(r.period_end, sep='~') }} | {{ r.duration_days }} | {{ r.duration_diff_days }} | {{ "{:,}".format(r.amount.krw) }} | {{ r.reason | default('') }} |
{% endfor %}

**1차수 공사 총 연장일수: {{ first_year_contract.revisions[-1].duration_days - first_year_contract.initial.duration_days }}일**
