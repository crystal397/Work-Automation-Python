# 공기연장 간접비 보고서 자동생성 시스템 — 의존성
# 설치: pip install -r requirements.txt

# ── generate 명령 필수 ──────────────────────────────────
python-docx>=1.1.0          # Word 문서 생성
openpyxl>=3.1.0             # Excel 파일 처리
pandas>=2.0.0               # 데이터 처리

# ── extract 명령 필수 (텍스트 추출) ────────────────────
pytesseract>=0.3.10         # OCR (Tesseract 엔진 별도 설치 필요)
Pillow>=10.0.0              # 이미지 처리
pdfplumber>=0.10.0          # PDF 텍스트 추출 (기본)
PyMuPDF>=1.23.0             # PDF 텍스트/OCR 추출 (폴백)
